# Setup

Bootstrap scripts for setting up a new machine.

## macOS

```sh
curl -fsSL https://raw.githubusercontent.com/b-hayes/setup/master/mac | zsh
```

Installs: Homebrew, Brave Browser, GitHub CLI, then runs `gh auth login`.
